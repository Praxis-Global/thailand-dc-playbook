# Gated report source docs — access is via the enquiry form, keep private
downloads/

# Local dev preview server (not needed on GitHub Pages)
server.ps1

# OG cover build source (the rendered og-image.png IS committed)
og-cover.html

# Converted markdown / scratch
*.md.bak
